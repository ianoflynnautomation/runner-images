name: 'Shebang Line Validator'
description: 'Ensures all script files have correct and secure shebang lines for Linux environments.'

runs:
  using: "composite"

  steps:
    - name: Check Shebang Lines
      run: ./images.CI/shebang-linter.ps1
      shell: pwsh
